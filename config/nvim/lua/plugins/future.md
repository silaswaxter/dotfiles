# Plugins I want:

## Integration Fast:

These are plugins which are relatively simple and can be added without worry about not using.

- render-markdown.nvim
- lualine.nvim
    - needs configuring (want simple useful non-obvious info) that is more valuable than the screen realestate being sacrificed for it.
- indent detection
- lewis6991/gitsigns.nvim
- monaqa/dial.nvim
    - this extends vim-motion for inc/dec of number 
- lukas-reineke/indent-blankline.nvim
    - colorize indentation scope--useful for keeping track of where you are in nested code


## Integration Slow, workflow-altering:

These are the plugins I want to add to my nvim and workflow, but I don't want to add them all at once because I want to actually integrate it as part of my workflow. Otherwise I will only use like 2% of the capabilities

- Shortcuts/productivity boosters:
    - L3MON4D3/LuaSnip
    - zenmode:
        - this would be useful if I'm trying to focus on a single file while preserving splits, windows, buffers, etc.
    - undotree
- Highlighting:
    - treesitter
        - rainbow deliminators
- Intellisense
    - status indicator: `fidget.nvim`
    - LSP installer easy: `Mason`?
    - LSP errors, etc: `trouble.nvim`?
    - Autocompletion:  `cmp.nvim`?
    - Linting:
        - `nvim-lint`
- Formatting:
    - conform.nvim
- Navigation:
    - Harpoon
    - telescope.nvim
- Testing:
    - neotest
- Peristence:
    - Save current "workspace" and "resume" workspace. Useful for repeating work across powercycles.
